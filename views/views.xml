<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="training__student.list">
      <field name="name">Training_Student list</field>
      <field name="model">training__student.training__student</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="training__student.action_window">
      <field name="name">Training_Student window</field>
      <field name="res_model">training__student.training__student</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="training__student.action_server">
      <field name="name">Training_Student server</field>
      <field name="model_id" ref="model_training__student_training__student"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Training_Student" id="training__student.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="training__student.menu_1" parent="training__student.menu_root"/>
    <menuitem name="Menu 2" id="training__student.menu_2" parent="training__student.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="training__student.menu_1_list" parent="training__student.menu_1"
              action="training__student.action_window"/>
    <menuitem name="Server to list" id="training__student" parent="training__student.menu_2"
              action="training__student.action_server"/>
    -->
  </data>
</openerp>